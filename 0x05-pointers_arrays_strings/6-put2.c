#inculde "main.h"
#incude <stdio.h>
/**
 * Puts2 - prints the first letter of the word.
 * @str: contains string
 * Return: string
 */
void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (i % 2 == 0)
		{
			_putchar(str[i];
		}
		_putchar('\n');
	}
}
