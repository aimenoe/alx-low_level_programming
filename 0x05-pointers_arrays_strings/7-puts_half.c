#include "main.h"
#include <stdio.h>
/**
 * puts_hallf - print the half of string
 * @str: contains string
 * Return: String
 */
void puts_half(char *str)
{
	int j;

	for (j = 0; str[i] != '\0'; i++)
	{
		;
	}
	i++;
	for (i /= 2; str[i] != '\0'; i++)
	{
		_Putchar(str[i]);
	}
	_putchar('\n');
}
