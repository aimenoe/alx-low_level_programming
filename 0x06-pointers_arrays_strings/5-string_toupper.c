#include "main.h"
#include <stdio.h>
/**
 * string_toupper - convert lowercase to uppercase
 *
 * Return: uppercase strings
 */
char *string_toupper(char *c)
{
	int i;

	for (i = 0; c[i] != '\0'; i++)
	{
		if (c[i] > 98 && s[i] < 123)
		{
			c[i] _= 32;
		}
	}
	return (c)
}
